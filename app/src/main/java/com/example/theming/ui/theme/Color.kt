package com.example.theming.ui.theme

import androidx.compose.ui.graphics.Color

/**
 *  Colors: Primary and Secondary.-
 *  A primary color is the color displayed most frequently across your app's screens and components.
 *  A secondary color provides more ways to accent and distinguish your product,
 *  Having a secondary color is optional
 *  Secondary colors are best for:
 *  Floating action buttons
 *  Selection controls, like sliders and switches
 *  Highlighting selected text
 *  Progress bars
 *  Links and headlines
 */

/**
 * Surface colors affect surfaces of components, such as cards, sheets, and menus.
 * The background color appears behind scrollable content. The baseline background and surface color is #FFFFFF.
 * Error color indicates errors in components, such as invalid text in a text field.
 * The baseline error color is #B00020.
 */

/**
 * "On Colors"
 * referring to the fact that they color elements that appear “on” top of surfaces
 * that use the following colors: a primary color, secondary color, surface color, background color, or error color
 * When a color appears “on” top of a primary color, it’s called an “on primary color".
 */

/**
 * Apps that use white text must have backgrounds that are accessible against white text.
 * These white check marks indicate when white text is accessible against various background color swatches.
 * The 400 color swatch is applied to this UI.
 */

/**
 * Apps that use black text must have backgrounds that are accessible against black.
 * These black check marks indicate when black text is accessible against various background color swatches.
 * The 50 color swatch is applied to this UI.
 */

// Theme 1: Primary and Secondary Color
val Purple200 = Color(0xFFBB86FC)
val Purple500 = Color(0xFF6200EE)
val Purple700 = Color(0xFF3700B3)
val Teal200 = Color(0xFF03DAC5)

// Theme 2
val Amber200 = Color(0xFFFFE082)
val Amber500 = Color(0xFFFFC107)
val Amber700 = Color(0xFFFFA000)
val Amber900 = Color(0xFFFF6f00)

val Red300 = Color(0xFFE57373)
val Red800 = Color(0xFFC62828)